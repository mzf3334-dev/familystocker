/**
 * Config for Family Stock Checker.
 * APPS_SCRIPT_URL: paste the Web app URL (ends in /exec) from the one-time
 * Apps Script setup (see google-apps-script/Code.gs). Leave empty to fall
 * back to read-only mode from the GitHub raw file.
 */
export const APPS_SCRIPT_URL = '';

export const GITHUB_OWNER = 'mzf3334-dev';
export const GITHUB_REPO = 'familystocker';
export const GITHUB_BRANCH = 'main';
export const DATA_PATH = 'data/inventory.json';
